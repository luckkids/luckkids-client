import { StyleSheet } from 'react-native';

export const defaultStyles = StyleSheet.create({
  container: {
    width: '100%',
    height: 52,
    borderRadius: 15,
  },
  textInput: {
    width: '100%',
    flex: 1,
  },
});
