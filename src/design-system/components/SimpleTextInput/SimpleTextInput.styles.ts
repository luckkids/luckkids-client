import { StyleSheet } from 'react-native';

export const defaultStyles = StyleSheet.create({
  container: {
    width: '100%',
    height: 60,
    borderRadius: 15,
    padding: 20,
  },
  textInput: {
    width: '100%',
    height: 65,
    flex: 1,
  },
});
